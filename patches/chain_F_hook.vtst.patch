--- a/src/chain.F
+++ b/src/chain.F
@@ -42,6 +42,7 @@
     USE main_mpi
     USE poscar
     USE lattice
+    USE ef_ts
     USE neb
     USE dynmat
     USE dimer
@@ -138,6 +139,9 @@
       TYPE(nh_chains) :: NHC
       TYPE (in_struct) :: IO
 
+      !--- VASP-EF hook: eigenvector-following TS optimizer (EF_TS=.TRUE.) ---
+      IF (ef_ts_step(nions,posion,toten,force,a,b,iu6)) RETURN
+
       INTEGER node
       REAL(q),ALLOCATABLE,SAVE :: force_all(:,:,:)
 
